name: Deploy to Railway

on:
  push:
    branches:
      - main
  workflow_dispatch: # Permite execução manual

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4
      
      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.12.0'
          cache: 'npm'
      
      - name: Instalar dependências
        run: npm ci
      
      - name: Build do projeto
        run: npm run build
      
      - name: Verificar build
        run: |
          if [ ! -d "dist" ]; then
            echo "Erro: Diretório dist não foi criado"
            exit 1
          fi
          echo "✅ Build concluído com sucesso!"
      
      - name: Instalar Railway CLI
        run: npm install -g @railway/cli@latest
      
      - name: Deploy para Railway
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          echo "🚀 Iniciando deploy para Railway..."
          # Verifica se o token está configurado
          if [ -z "$RAILWAY_TOKEN" ] || [ "$RAILWAY_TOKEN" == "" ]; then
            echo "⚠️ Aviso: RAILWAY_TOKEN não está configurado nos secrets do GitHub"
            echo "📝 Para configurar:"
            echo "   1. Acesse: https://github.com/vinicius08oliveira85/Analisedia/settings/secrets/actions"
            echo "   2. Clique em 'New repository secret'"
            echo "   3. Nome: RAILWAY_TOKEN"
            echo "   4. Valor: Cole o token do Railway (obtenha em https://railway.app/dashboard)"
            echo "❌ Deploy cancelado - configure o secret primeiro"
            exit 1
          fi
          echo "✅ Token configurado, continuando deploy..."
          # Linka o projeto ao Railway (se necessário)
          if [ -n "${{ secrets.RAILWAY_PROJECT_ID }}" ] && [ "${{ secrets.RAILWAY_PROJECT_ID }}" != "" ]; then
            echo "Linking projeto: ${{ secrets.RAILWAY_PROJECT_ID }}"
            railway link ${{ secrets.RAILWAY_PROJECT_ID }} || true
          fi
          # Faz o deploy
          echo "Fazendo deploy..."
          if [ -n "${{ secrets.RAILWAY_SERVICE_ID }}" ] && [ "${{ secrets.RAILWAY_SERVICE_ID }}" != "" ]; then
            railway up --service ${{ secrets.RAILWAY_SERVICE_ID }}
          else
            railway up
          fi
          echo "✅ Deploy concluído com sucesso!"

